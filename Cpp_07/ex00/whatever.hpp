#pragma once

#include <iostream>

using std::cout;
using std::endl;
using std::string;

template <typename T>
void swap(T &a, T &b);

template <typename M>
M min(M a, M b);

template <typename N>
N max(N a, N b);